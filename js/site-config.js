/* Site-wide config — edit these two values after you create Google accounts.
   GA4: https://analytics.google.com → Admin → Data streams → Web → Measurement ID (G-…)
   Search Console: https://search.google.com/search-console → Add property → HTML tag method
   Paste only the content= value from the meta tag (not the whole tag). */
window.KV_SITE = {
  gaMeasurementId: "",
  gscVerification: ""
};
